images
- capture ![]() with regex and wrap with `<div class="img-box">$</div>`
  - if ![](.gif), wrap with `<div class="img-box animated">$</div>` instead.
vtranslit
- capture //*// and replace with `<span class="vtranslit scheme-deva" lang="sa">$</span>`
custom blocks
  prakriya
    - capture
;;;prakriya\n * \n;;; and replace with `<div class="prakriya-block">`.
transclusion
- capture {{(relative) url}} and replace wih content from url.
